<?php

  function connect_db() {
    $db = new mysqli('localhost', 'impusers', 'MYSQL-PASSWORD');
    if (mysqli_connect_errno()) { return NULL; }
    if (!$db->select_db('impusers')) { return NULL; }
    return $db;
  }

?>
